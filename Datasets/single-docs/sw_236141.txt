XOR gate

The XOR gate (Exclusive OR) is a logic gate that outputs 1 (true) when exactly one of its inputs is 1 (true). That means that if both of its inputs are the same, the output will be 0 (false).

There are three symbols for the XOR gate:


